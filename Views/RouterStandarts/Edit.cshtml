@model RouterLab.RouterStandart

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RouterStandart</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RouterStandartId" />
            <div class="form-group">
                <label asp-for="RouterId" class="control-label"></label>
                <select asp-for="RouterId" class="form-control" asp-items="ViewBag.RouterId"></select>
                <span asp-validation-for="RouterId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StandartId" class="control-label"></label>
                <select asp-for="StandartId" class="form-control" asp-items="ViewBag.StandartId"></select>
                <span asp-validation-for="StandartId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
